[package]
name = "ge_game"
version = "0.1.0"
edition = "2024"
publish = false

[features]
export = ["dep:ts-rs"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
ts-rs = { version = "12.0.1", features = ["serde-compat"], optional = true }
