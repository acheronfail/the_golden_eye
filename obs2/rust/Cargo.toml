[workspace]
members = ["catalog", "clip", "core", "cv", "game", "media", "settings"]
default-members = ["core"]
resolver = "3"

[profile.dist]
inherits = "release"
# Reducing binary size. See: https://github.com/johnthagen/min-sized-rust
strip = true
lto = true
codegen-units = 1
