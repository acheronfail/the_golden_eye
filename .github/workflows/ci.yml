name: CI

on:
  pull_request:
  push:
    branches:
      - master
      - acheronfail/simplifications
      - "codex/ci-*"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build-packages.yml
    secrets: inherit
